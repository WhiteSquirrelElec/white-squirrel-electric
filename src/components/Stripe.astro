<!-- Diagonal stripe ribbon — visual punctuation between hero and ticker -->
<div class="stripe" aria-hidden="true"></div>

<style>
  .stripe {
    height: 18px;
    background: repeating-linear-gradient(
      135deg,
      var(--red)  0px,
      var(--red)  14px,
      #fff        14px,
      #fff        22px,
      var(--blue) 22px,
      var(--blue) 36px,
      #fff        36px,
      #fff        44px
    );
  }
</style>
